@extends('layouts.app')
@section('content')<div class="container"><span class="eyebrow">مدیریت</span><h1>مرکز کنترل فراست</h1><div class="admin-grid"><section class="panel"><h2>قوانین قیمت‌گذاری</h2>@foreach($rules as $rule)<form class="rule" method="post" action="/admin/pricing">@csrf<input type="hidden" name="key" value="{{ $rule->key }}"><input name="label" value="{{ $rule->label }}"><input type="number" name="value" value="{{ $rule->value }}"><button>ذخیره</button></form>@endforeach</section><section class="panel"><h2>کاربران</h2>@foreach($users as $u)<div class="doc"><span>{{ $u->mobile }}</span><span>{{ $u->role }}</span>@if(!$u->isAdmin())<form method="post" action="/admin/users/{{ $u->id }}/block">@csrf<button>{{ $u->is_blocked?'رفع مسدودی':'مسدود کردن' }}</button></form>@endif</div>@endforeach</section></div></div>@endsection
