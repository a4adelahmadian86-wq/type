@extends('layouts.app')
@section('content')
<div class="finance-page" dir="rtl"><div class="finance-head"><div><span class="eyebrow">MASTER BRAND</span><h1>مدیریت نمادهای ارتباطی</h1><p>تا ۸ نماد قابل تعریف است؛ فوتر فقط سه مورد اول را نمایش می‌دهد و در صورت بیشتر بودن، لینک «همه نمادها» را نشان می‌دهد.</p></div><a class="finance-back" href="/admin"><i class="fa-solid fa-arrow-right"></i> مدیریت</a></div><form method="post" action="{{ route('admin.social.update') }}" class="checkout-card social-admin-form">@csrf @for($i=0;$i<8;$i++)@php($link=$links[$i]??[])<div class="social-admin-row"><span class="tx-icon credit"><i class="fa-solid fa-link"></i></span><input name="links[{{ $i }}][title]" value="{{ $link['title']??'' }}" placeholder="عنوان"><input name="links[{{ $i }}][url]" value="{{ $link['url']??'' }}" placeholder="https://..."><input name="links[{{ $i }}][icon]" value="{{ $link['icon']??'fa-solid fa-link' }}" placeholder="fa-brands fa-telegram"></div>@endfor<button class="pay-button" type="submit"><i class="fa-solid fa-floppy-disk"></i> ذخیره نمادها</button></form></div>
@endsection
