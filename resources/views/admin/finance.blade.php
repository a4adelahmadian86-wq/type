@extends('layouts.app')
@section('content')
<div class="finance-page" dir="rtl"><div class="finance-head"><div><span class="eyebrow">MASTER FINANCE</span><h1>مرکز مالی و پرداخت</h1><p>مالیات، درگاه، کیف پول و اصلاحات مالی کاربران از یک نقطه کنترل می‌شود.</p></div><a class="finance-back" href="/admin"><i class="fa-solid fa-arrow-right"></i> داشبورد مدیریت</a></div>
@if(session('status'))<div class="finance-alert"><i class="fa-solid fa-circle-check"></i>{{ session('status') }}</div>@endif
<div class="wallet-grid"><section class="checkout-card"><div class="card-title"><span><i class="fa-solid fa-scale-balanced"></i></span><div><h2>مالیات و قوانین قیمت</h2><small>مقادیر در سفارش ذخیره می‌شوند تا فاکتور قابل پیگیری باشد.</small></div></div><form method="post" action="{{ route('admin.finance.update') }}" class="topup-form">@csrf<label>نرخ مالیات و عوارض به درصد<input type="number" name="tax_rate_percent" min="0" max="100" step="0.01" value="{{ $taxRate }}" required></label><label style="margin-top:12px"><span><input type="checkbox" name="tax_enabled" value="1" {{ $taxEnabled ? 'checked' : '' }}> مالیات در فاکتور فعال باشد</span></label><label style="margin-top:12px">درگاه پرداخت<select name="payment_gateway"><option value="none" selected>درگاه هنوز متصل نشده</option></select></label><label style="margin-top:12px">شناسه پذیرنده<input type="password" name="gateway_merchant_id" placeholder="در صورت اتصال درگاه وارد کنید" autocomplete="new-password"></label><button class="pay-button" type="submit"><i class="fa-solid fa-floppy-disk"></i> ذخیره تنظیمات مالی</button></form></section>
<section class="checkout-card"><div class="card-title"><span><i class="fa-solid fa-user-lock"></i></span><div><h2>امنیت خروجی</h2><small>ملاک نهایی، پرداخت موفق سفارش است.</small></div></div><div class="credit-note"><i class="fa-solid fa-lock"></i><p>برای کاربر عادی، سرور فقط وقتی Word یا PDF را تحویل می‌دهد که سفارش پرداخت شده باشد و هش نسخه سند با نسخه پرداخت‌شده یکسان باشد. تغییر متن بعد از پرداخت، پرداخت قبلی را برای نسخه جدید معتبر نمی‌کند.</p></div><div class="credit-note"><i class="fa-solid fa-gift"></i><p>اعتبار رایگان بر اساس شماره موبایل هش‌شده و هفته جاری ثبت می‌شود و فقط هنگام تسویه سفارش مصرف می‌شود.</p></div></section></div>
<section class="checkout-card transactions"><div class="card-title"><span><i class="fa-solid fa-money-bill-transfer"></i></span><div><h2>اصلاح موجودی کاربران</h2><small>برای تست، پشتیبانی و اصلاحات حسابداری با ثبت دفترکل.</small></div></div><div class="transaction-list">@foreach($users as $u)<div class="transaction-row"><span class="tx-icon credit"><i class="fa-solid fa-user"></i></span><div><b>{{ $u->name ?: 'کاربر بدون نام' }}</b><small>{{ $u->mobile }} · موجودی {{ number_format($u->wallet?->balance_rials ?? 0) }} ریال</small></div>@if(!$u->isAdmin())<form method="post" action="{{ route('admin.finance.wallet',$u) }}" style="display:flex;gap:7px;align-items:center">@csrf<select name="type"><option value="credit">افزایش</option><option value="debit">کسر</option></select><input type="number" name="amount_rials" min="1000" max="10000000000" placeholder="مبلغ" required><input type="text" name="description" maxlength="500" placeholder="شرح"><button class="pay-button" style="width:auto;padding:8px 12px">ثبت</button></form>@endif</div>@endforeach</div></section></div>
@endsection
