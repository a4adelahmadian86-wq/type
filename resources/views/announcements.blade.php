@extends('layouts.app')
@section('content')
<section class="announcement-page" dir="rtl"><div class="announcement-page-head"><span class="announcement-page-icon"><i class="fa-solid fa-bullhorn"></i></span><div><span class="eyebrow">FARAST UPDATES</span><h1>اعلانات و اطلاع‌رسانی</h1><p>آخرین خبرها، تغییرات سرویس و اطلاعیه‌های مهم فراست.</p></div></div><div class="announcement-page-list">@forelse($announcements as $announcement)<article class="public-announcement type-{{ $announcement->type }}"><div class="public-announcement-icon"><i class="fa-solid {{ $announcement->type==='danger'?'fa-triangle-exclamation':($announcement->type==='warning'?'fa-circle-exclamation':($announcement->type==='success'?'fa-circle-check':'fa-circle-info')) }}"></i></div><div><div class="public-announcement-meta"><span>{{ $announcement->created_at?->format('Y/m/d') }}</span><span>{{ $announcement->type==='danger'?'مهم':($announcement->type==='warning'?'هشدار':($announcement->type==='success'?'موفقیت':'اطلاع‌رسانی')) }}</span></div><h2>{{ $announcement->title }}</h2><p>{{ $announcement->body }}</p></div></article>@empty<div class="empty-public-announcements"><i class="fa-regular fa-bell-slash"></i><b>اعلان فعالی وجود ندارد</b><span>در صورت انتشار اطلاعیه جدید، اینجا نمایش داده می‌شود.</span></div>@endforelse</div>{{ $announcements->links() }}</section>
@endsection
